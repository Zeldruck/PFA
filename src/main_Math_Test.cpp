#include "my_lib/Core/Maths/OrientedBox.hpp"
#include "my_lib/Core/Maths/Vec4.h"
#include "my_lib/Core/Maths/Cylinder.hpp"
#include "my_lib/Core/Maths/Collision.hpp"

int main2()
{
    /*OrientedBox A = OrientedBox(Vec3(5,5,5));
    OrientedBox B = OrientedBox(Vec3(5,5,5));
    Sphere sphere = Sphere(Vec3(0,0,0),5);

    A.position = Vec3(7.49,0,0);

    //std::cout << OrientedBox::IsOrientedBoxCollidingWithOrientedBox(A,B) << std::endl;

    //std::cout << IsSphereCollidingWithOrientedBox(sphere, A) << std::endl;

    Segment seg = Segment(Vec3(2,0,0), Vec3(1,0,0), 5);
    FiniteCylinder cyl = FiniteCylinder(Vec3(0,0,0), Vec3(0,0,1), 5, 10);
    Hit hit;
    hit.point = Vec3(0,0,0);

    std::cout << "There are a collisions ?: " << IsSegmentIntersectingFiniteCylinder(seg, cyl, hit) << ", and the collisions point is : X = " << hit.point.x << ", Y = " << hit.point.y << ", Z = " << hit.point.z << ";" << std::endl; 
    */return 0;
}